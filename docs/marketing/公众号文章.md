# 我做了一个 Chrome 插件，解决跨设备密码同步的痛点

> 本文作者：咕咚同学 | SlideNote 作者

---

## 起因

你是不是也有这种经历：

在公司电脑上存了阿里云的 AccessKey，回家想登录时怎么也想不起来；
把团队的 API Key 存在微信里，换手机后找不到了；
把常用账号密码记在备忘录里，结果几台设备之间不同步。

我也一样。所以我做了一个简单的解决方案——**SlideNote**。

---

## 什么是 SlideNote？

**SlideNote** 是一个 Chrome 浏览器侧边栏笔记插件：

- 打开浏览器就能看到，点击图标即可展开
- 你存的密码、API Key、服务器地址，自动同步到所有设备
- 极简设计，没有多余功能

![SlideNote 界面](https://github.com/maoruibin/SlideNote/raw/main/docs/assets/screenshot.png)

---

## 为什么做这个工具？

市面上笔记产品很多：Notion、Obsidian、印象笔记……

但它们对我来说都太「重」了。

我只是想：
- 快速存个密码
- 换台电脑还能看到
- 不要打开一个新的 App

所以我用了 Chrome 的 Side Panel API，做了一个「住在浏览器侧边」的便利贴。

---

## 技术上的选择

**我甚至没有用 React/Vue。**

为什么呢？

```
React 体积: ~100KB+
SlideNote 体积: ~15KB
```

对于一个 CRUD 功能来说，框架的收益远小于成本。

我用了：
- Vanilla JS（原生 JavaScript）
- Chrome Storage Sync API（自带云同步）
- CSS Variables（设计系统）

代码量很小，维护成本很低，十年后依然能跑。

---

## 一些细节

### 自动保存

你不需要点「保存」。停止输入 1 秒后，自动保存。

### 删除确认

防止手滑，删除按钮要等 3 秒才能点击。

### 跨设备同步

基于 Chrome 的同步机制，你在公司存的笔记，回家就能看到。

### 冲突处理

如果两台设备同时编辑，会自动合并，用时间戳更新的版本。

---

## 开源原因

**这东西太简单了，不开源说不过去。**

而且，涉及数据同步的产品，不开源很难获得信任。

代码已经在 GitHub 上，MIT 协议，欢迎围观：

👉 https://github.com/maoruibin/SlideNote

---

## 如何使用

### 方式一：开发者模式

```bash
git clone https://github.com/maoruibin/SlideNote.git
cd slide-note
npm install
npm run build
```

然后在 Chrome 中加载 `dist` 目录。

### 方式二：Chrome 应用商店（计划中）

正在准备上架，上架后搜索「SlideNote」即可。

---

## 未来规划

v0.0.1 只是 MVP。接下来计划：

- **归档功能**：解决 Chrome Storage 的 100KB 限制
- **数据导出**：支持导出 JSON/Markdown
- **快捷键**：更高效的操作

如果你有什么想法，欢迎提 Issue 或 PR。

---

## 写在最后

这是我第一次做 Chrome 插件，代码可能还有很多不完善的地方。

但它是「能用」的——我自己每天都在用。

如果你也有类似的痛点，不妨试试 SlideNote：

> **Simple notes, always by your side**

> 侧边笔记，常伴左右

---

*作者：咕咚同学 | GitHub: https://github.com/maoruibin/SlideNote*
